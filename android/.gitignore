# 可复现构建：gradlew/gradle-wrapper.jar 必须入库（与本项目此前工程同款处理）
# 否则 CI 检出后无 gradlew，`./android/gradlew` 报 command not found (127)。
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
